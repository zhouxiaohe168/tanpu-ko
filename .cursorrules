# 探铺（TanPu）— 连锁品牌智能选址数据平台

## 🎯 项目概述
探铺是一个帮助连锁品牌加盟商通过数据找到品牌空白点位的付费信息平台。
核心功能：输入目标品牌后，自动识别周边有头部竞品但目标品牌尚未覆盖的位置，结合配套设施、在建项目等数据评分，并输出 AI 研判。
商业模式：按次查询付费 + 会员订阅 + AI 报告付费。

## 🏗️ 技术栈（严格遵守）
- 前端：Next.js 14（App Router）+ TypeScript（strict）+ TailwindCSS + shadcn/ui + 高德地图 JS API 2.0 + Zustand + Axios
- 后端：Python 3.11+ + FastAPI + SQLAlchemy 2.0 async + Alembic + PostgreSQL 15 + Redis 7 + Celery + Pydantic v2 + PyJWT + httpx
- 外部服务：高德 Web 服务 API、OpenAI 兼容 API、微信支付 V3、阿里云 SMS、阿里云 OSS

## 🌐 API 规范
- 前缀：`/api/v1/`
- 鉴权：Bearer JWT
- 响应格式：`{"code":0,"data":{},"message":"success"}`
- 错误码：`0` 成功，`1xxx` 参数，`2xxx` 业务，`3xxx` 认证，`5xxx` 系统
- 金额单位：分（整数）
- 坐标系：GCJ-02
- 分页：`page` + `page_size`，返回 `total`

## 🎨 前端规范
- 移动优先（Mobile First）
- 主色 `#2563EB`，强调色 `#F59E0B`，成功色 `#10B981`，警告色 `#EF4444`
- 统一圆角：`rounded-lg`
- 组件：TypeScript + React.FC，Props 独立 interface，优先 shadcn/ui，样式仅用 Tailwind

## ⚙️ 后端规范
- Python PEP 8，函数必须类型注解
- 全面 async/await
- 业务逻辑在 `services/`，API 层只做参数校验和调度
- 配置仅通过 `.env`

## 📍 地图与计算
- 高德 API：`/v3/place/text`、`/v3/place/around`
- 每次请求最多 25 条，必须分页
- 结果优先缓存数据库，���少重复调用
- 距离计算统一 Haversine，单位米

## 🧠 评分规则（总分 100）
- 品牌验证 40 + 配套设施 35 + 未来潜力 15 + 风险调整（±10）
- 评级：S(>=90) / A(>=75) / B(>=60) / C(>=40) / D(<40)

## 🤖 AI 报告规范
- System: 资深商业选址分析师，要求数据支撑、可给出不建议结论、必须包含风险提示
- 输出结构：一句话结论 → 竞争格局 → 客群画像 → 时段分析 → 趋势判断 → 风险提示 → 经营建议
- 必须附免责声明
- 默认模型：`deepseek-chat`，高级报告可用 `gpt-4o`
- 温度 `0.7`，最大 `2000 tokens`
- 相同位置+品牌缓存 24 小时

## 🛡️ 安全规范
- 密码 bcrypt 哈希
- JWT：access 2 小时，refresh 7 天
- API 限流：未登录 10/min，免费用户 30/min，会员 120/min
- ORM 参数化查询防注入
- CORS 仅允许前端域名
- 支付回调必须验签
- `.env` 严禁提交